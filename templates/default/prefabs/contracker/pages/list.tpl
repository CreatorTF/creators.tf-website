<div
    class="page[VISIBLE] visible[/VISIBLE]"
    data-title="{title}"
    data-parent="{parent}"
    data-dir="{dir}"
>
    <div class="page-background" style="background-image: url({background});"></div>
</div>
